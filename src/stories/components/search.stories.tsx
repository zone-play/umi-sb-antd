import React from 'react';
import { SearchComponent } from '../../components/Search';

export default {
  title: 'Components',
  component: SearchComponent,
};

const Template = () => <SearchComponent />;

export const Search = Template.bind({});
